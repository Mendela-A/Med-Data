# TODO

## Інфраструктура / БД

- [ ] **PostgreSQL контейнер** — додати `db` сервіс у `docker-compose.yml` (postgres:16-alpine); один контейнер, кілька баз
- [ ] **Per-branch databases** — `app_main`, `app_statisty` тощо; перемикання через `.env`
- [ ] **`scripts/use-branch.sh`** — перемикання бази при зміні гілки (оновлює `.env`, створює БД якщо нема)
- [ ] **`scripts/refresh-from-main.sh`** — витягує SQLite з named volume main → конвертує в PostgreSQL через `migrate_sqlite_to_postgres.py`
- [ ] **`scripts/dump-branch-db.sh`** — pg_dump поточної бази в `dumps/`
- [ ] **Оновити `scripts/load-db.sh`** — підтримка `.sql` (PostgreSQL) + `.db` (SQLite→конвертація)
- [ ] **`entrypoint.sh`** — прибрати SQLite-перевірки (`sqlite_master`, `-f app.db`), замінити на postgres-ready логіку
- [ ] **`models.py`** — видалити `_set_sqlite_pragma()` та `init_db_events()` (SQLite WAL pragma)
- [ ] **`requirements.txt`** — додати `psycopg2-binary>=2.9`
- [ ] **`Dockerfile`** — замінити `sqlite3` на `postgresql-client`
- [ ] **Міграція даних** — запустити `scripts/migrate_sqlite_to_postgres.py` (перевірити підтримку нових таблиць: ambulatory_records, daily_reports, print_settings)
- [ ] **`.gitignore`** — додати `dumps/`

## UI / UX

- [ ] **Breadcrumbs** — наповнити `{% block breadcrumbs %}` на ключових сторінках (Форма 007 → День → Редагування)
- [ ] **Empty states** — `.empty-state` компонент у таблиці без даних (Records, Ambulatory, НСЗУ)
- [ ] **Mobile card-view** — Records і Ambulatory на малих екранах замість широкої таблиці

## Функціональність

- [x] **Нові відділення в import-скриптах** — Кардіохірургічне, Хірургічне дит., Урологічне дит., Травматологічне дит. присутні й узгоджені в `NAME_MAP` усіх скриптів (`import_q1_2026.py`, `import_april_007.py`, `import_may_007.py`, `import_june_007.py`); `import_007.py` без NAME_MAP за задумом
- [ ] **Тести для амбулаторки** — розширити `tests/test_ambulatory.py`

## Технічний борг

- [x] **Дублікат CSS** — у `static/style.css` подвійна секція `.month-picker` — видалено мертвий перший блок, лишився робочий (з `.mp-month-btn.disabled`)
- [x] **`migrations/versions/e30793237851`** — `downgrade()` перевірено: чисте дзеркало `upgrade()` (лише `ambulatory_records` + індекси), жодних зайвих drop'ів

---

# UI/UX — Перспективний план розвитку та покращення інтерфейсу

Цей файл містить список узгоджених ідей та планів щодо вдосконалення інтерфейсу, зручності роботи персоналу та аналітичних можливостей системи.

---

## 1. Виконано (Done) 🚀

- [x] **Глобальна інтеграція HTMX та Alpine.js**
  - Підключено офіційні легкі CDNs для HTMX та Alpine.js у головному шаблоні `<head>` (`templates/base.html`), що робить ці фреймворки доступними на будь-якій сторінці додатку.

- [x] **Навігація стрілками клавіатури (Excel-style) у Формі 007**
  - Створено JavaScript-модуль у формі редагування добового звіту (`templates/statisty/form007_edit.html`).
  - Реалізовано перехід між усіма полями введення за допомогою стрілок ($\uparrow$, $\downarrow$, $\leftarrow$, $\rightarrow$), а також клавіш `Enter` (для переходу на аналогічну колонку наступного рядка).
  - Налаштовано автоматичне виділення (`select()`) всього тексту при фокусуванні в полі введення, що дозволяє миттєво змінювати цифри без натискання backspace.

- [x] **Інтерактивна валідація даних та розрахунок "на льоту" у Формі 007**
  - Додано автоматичний клієнтський перерахунок значень стовпчика 14 («Знаходилось хворих на кінець доби») у реальному часі за формулою: `Було хворих на початок (5) + Поступило (6) + Переведено з інших відділень (9) - Переведено в інші (10) - Виписано (11) - Померло (13)`.
  - Реалізовано колірні індикатори статусу ліжко-місць:
    * **М'який помаранчевий колір** — якщо значення хворих на кінець стає від'ємним (сигналізує про помилку в розрахунках).
    * **М'який червоний колір** — якщо кількість хворих перевищує ліжковий фонд відділення (перевантаження).
    * **М'який жовто-бежевий колір** — при коректному балансі.

- [x] **Інтерактивні графіки та діаграми (Chart.js) на сторінці Статистики**
  - Інтегровано сучасну бібліотеку Chart.js для візуалізації показників реєстру виписок на сторінці «Статистика» (`templates/admin_statistics.html`).
  - Додано 3 красиві інтерактивні діаграми:
    1. **Кругову діаграму (Doughnut Chart)** — «Розподіл статусів виписок» (відсоткове співвідношення статусів опрацьованих, виписаних, померлих та порушених записів).
    2. **Горизонтальну гістограму (Horizontal Bar Chart)** — «Виписки по відділеннях» (порівняння завантаженості відділень).
    3. **Вертикальну гістограму (Bar Chart)** — «Сума по групах АДСГ (Top-5)» (фінансовий звіт по групах АДСГ з автоматичним форматуванням тисячних чисел).
- [x] **Преміальний нічний режим (Dark Mode) для втомлених очей**
  - Додано глобальний перемикач теми (іконки сонечка/місяця) у правому кутку навігаційної панелі `templates/base.html`.
  - Впроваджено розширені CSS-змінні у `static/style.css` для ідеальної адаптації всіх елементів (карток, таблиць, полів вводу, кнопок, модальних вікон).
  - Створено inline-скрипт ініціалізації теми у `<head>` для повного виключення білого спалаху при перезавантаженні сторінки (FOUC).
  - Синхронізовано 3 аналітичні діаграми **Chart.js** на сторінці Статистики: кольори легенд, підписів та сіток оновлюються в реальному часі за подією `theme-changed` без перезавантаження сторінки.
  - Реалізовано семантичні CSS-класи для валідації Excel-таблиці у **Формі 007** (`.excel-warning`, `.excel-danger`, `.excel-computed`) замість жорстких JS-інлайнів, що гарантує відмінну читабельність і м'які кольори у темній темі.
---

## 2. Наступні кроки для покращення інтерфейсу (Next Steps) 📋

- [ ] **Живий "розумний" пошук (Fuzzy Search) у Реєстрі виписок через HTMX**
  - **Чому це важливо:** Дозволить операторам миттєво знаходити потрібну картку пацієнта без очікування повного перезавантаження сторінки.
  - **План реалізації:**
    1. На полі пошуку `templates/records/records.html` додати HTMX-атрибути:
       `hx-get="{{ url_for('records.index') }}" hx-trigger="keyup delay:200ms, search" hx-target="#records-table-body" hx-indicator="#search-spinner"`.
    2. У роуті бекенду `/records` перевіряти заголовок `HX-Request`. Якщо запит прийшов через HTMX, замість рендерингу всієї сторінки повертати лише частковий HTML-шаблон (partial template) `_records_table_rows.html`.


